{
  "name":"xmrpoolui",
  "description": "UI for XMR Pool.net",
  "repository": "",
  "license": "?",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "update-deps": "npm update",
    "postupdate-deps": "bower update",

    "prestart": "npm install",
    "start": "http-server -p 8000 -c-1 ./app"
  }
}
